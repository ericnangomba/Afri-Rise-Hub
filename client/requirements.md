## Packages
framer-motion | Page transitions and scroll-triggered animations
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging tailwind classes

## Notes
- Base stack includes react-hook-form, @hookform/resolvers, lucide-react, and zod.
- We use custom Google Fonts: Plus Jakarta Sans for body text, and Outfit for display headings.
- The `/api/contacts` endpoint accepts contact form submissions, expecting specific inquiry types.
